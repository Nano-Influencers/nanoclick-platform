import 'package:flutter/material.dart';
class TreasureHistory extends StatelessWidget { const TreasureHistory({super.key}); @override Widget build(BuildContext context) => const Center(child: Text('Treasure history is currently unavailable while this feature is migrated to the backend.')); }
