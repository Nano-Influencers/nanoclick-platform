fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
pydantic==2.7.1
pydantic-settings==2.2.1
pydantic[email]==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend detection reads bcrypt.__about__.__version__,
# which bcrypt>=4.1 removed entirely — this breaks *all* password hashing at
# runtime (ValueError from passlib's internal self-test, not from the actual
# password). Pin to the last bcrypt release passlib 1.7.4 works with.
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.4
celery==5.4.0
boto3==1.34.102
httpx==0.27.0
python-dotenv==1.0.1
Pillow>=10.4.0
imagehash>=4.3.1
authlib==1.3.0
itsdangerous==2.2.0
